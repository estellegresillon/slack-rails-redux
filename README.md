RAILS + REACT

```
$rails new 
	--database postgresql
	--webpack=react 
	-m https://raw.githubusercontent.com/lewagon/rails-templates/master/minimal.rb
	PROJECT NAME

$ nom install
$ bundle
$ rails db:create
$ rails db:migrate
$ rails db:seed
$ rails s
```
